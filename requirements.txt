# Pinned to tested versions. For GPU, install a CUDA torch build separately
# (see CLAUDE.md / README) — torch is intentionally left unpinned here.
torch>=2.0.0
faster-whisper==1.2.1
deep-translator==1.11.4
flask==3.1.3
flask-sock==0.7.0
werkzeug==3.1.6
numpy==2.4.3
